import React from "react";
import { Typography } from "./Typography";
export declare const Heading: ({ as, children, ...props }: Omit<React.ComponentProps<typeof Typography>, "type">) => JSX.Element;
